FILE="./api/.env"

cat <<EOM >$FILE
REDIS_HOST  = $REDIS_HOST
REDIS_PORT  = $REDIS_PORT
PGHOST      = $PGHOST
PGPORT      = $PGPORT
PGUSER      = $PGUSER
PGPASSWORD  = $PGPASSWORD
PGDATABASE  = $PGDATABASE

EOM

touch FILE
cat FILE

exit 0
